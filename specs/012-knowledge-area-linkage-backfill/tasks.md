---

description: "Task list for feature implementation"
---

# Tasks: Knowledge Area Linkage Backfill

**Input**: Design documents from `/specs/012-knowledge-area-linkage-backfill/`

**Prerequisites**: plan.md, spec.md, research.md, data-model.md, quickstart.md

**Tests**: Included — the spec's success criteria (SC-005, SC-006) are directly about data integrity across repeated runs, and this codebase's constitution (Principle IV, Audit-Driven Data Quality) requires new loaders/mergers to include audit/test coverage.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: Which user story this task belongs to (US1, US2, US3)

## Path Conventions

Single project — `src/`, `tests/` at repository root, per plan.md.

---

## Phase 1: Setup

**Purpose**: Confirm the current broken state is captured before any change, so the fix's effect is measurable.

- [x] T001 Run the read-only diagnostic query from `specs/012-knowledge-area-linkage-backfill/quickstart.md` step 1 against `db/horizon.db` and record baseline counts (expected: 0 rows in `researcher_knowledge_areas`, `group_knowledge_areas`, `initiative_knowledge_areas`)

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Fix the backup provisioner and merger — every user story depends on the junction tables actually being populated and propagated.

**⚠️ CRITICAL**: No user story can be verified until this phase is complete.

- [x] T002 Add sub-step "11e. Knowledge Area Associations" in `src/core/logic/backup_db_provisioner.py` (after the existing "11d" junction block, ~line 320+): iterate `researchers`, `research_groups`, and `initiatives` records loaded from the canonical archive, and for each nested `knowledge_areas` entry, `INSERT OR IGNORE INTO researcher_knowledge_areas (researcher_id, area_id)`, `INSERT OR IGNORE INTO group_knowledge_areas (group_id, area_id)`, `INSERT OR IGNORE INTO initiative_knowledge_areas (initiative_id, area_id)` respectively — mirroring the existing pattern used for `article_authors` (lines 251-259)
- [x] T003 In the same new sub-step (T002), skip and count (do not insert) any nested `knowledge_areas` entry whose `id` does not exist in the already-loaded `knowledge_areas` header table, per FR-008 — accumulate a `skipped_knowledge_area_links` counter and log it at the end of `_build_database_from_archive` in `src/core/logic/backup_db_provisioner.py`
- [x] T004 [P] Add `"researcher_knowledge_areas"` and `"initiative_knowledge_areas"` to the `tables_to_merge` list in `src/core/logic/backup_merger.py` (~line 72-90), alongside the existing `"group_knowledge_areas"` entry
- [x] T005 [US-shared] [P] Add unit tests in `tests/test_backup_merger.py`: extend the `temp_env` fixture's dummy zip with `knowledge_areas_canonical.json` and nested `"knowledge_areas"` arrays on researcher/group/initiative records, then assert `BackupDatabaseProvisioner` populates all three junction tables and `BackupDatabaseMerger.merge()` reports non-zero counts for `researcher_knowledge_areas`, `group_knowledge_areas`, and `initiative_knowledge_areas`
- [x] T006 [P] Add a unit test in `tests/test_backup_merger.py` asserting that a nested `knowledge_areas` entry referencing a non-existent `area_id` is skipped without raising and without inserting a dangling row (FR-008)

**Checkpoint**: Junction tables populate correctly from provisioning through merge — User Stories 1 and 2 can now be verified.

---

## Phase 3: User Story 1 - Knowledge Areas Visible on Researcher, Group, and Initiative Pages (Priority: P1) 🎯 MVP

**Goal**: Every researcher, research group, and initiative profile on the Dashboard displays its real knowledge areas instead of none.

**Independent Test**: After Phase 2 fixes and a fresh `init_backup_db` + `merge_backup` + `export_canonical` run, open the exported data for a known entity (e.g. researcher id 14, "Pablo Rodrigues Muniz") and confirm its knowledge areas are present.

### Implementation for User Story 1

- [x] T007 [US1] Run `python app.py init_backup_db` then `python app.py merge_backup` against a working copy of `db/horizon.db` and confirm via the query in `quickstart.md` step 4 that all three junction tables are non-empty and counts are consistent with the nested arrays in `data/exports/novo_backup.zip`
- [x] T008 [US1] Run `python app.py export_canonical` and verify `data/exports/knowledge_areas_mart.json` (generated by `KnowledgeAreaMartGenerator` in the mart generator module) is no longer `[]`
- [x] T009 [US1] Verify `data/exports/initiatives_analytics_mart.json` entries now contain non-empty `"knowledge_areas"` arrays for initiatives known to have them (e.g. initiative id 1)
- [x] T010 [US1] Verify `data/exports/researchers_canonical.json` and `data/exports/research_groups_canonical.json` entries include populated `knowledge_areas` for records known to have them (spot-check researcher id 14 and group id 1 against the samples documented in `research.md`)

**Checkpoint**: User Story 1 is fully functional and independently verifiable — knowledge areas render across all three entity types.

---

## Phase 4: User Story 2 - Knowledge Area Links Survive Weekly Runs and Backup Merges (Priority: P2)

**Goal**: The fix from Phase 2/3 is durable — it does not regress on the next weekly run.

**Independent Test**: Run the full weekly pipeline twice in sequence and confirm junction table counts remain stable or grow, never returning to 0.

### Implementation for User Story 2

- [x] T011 [US2] Add an integration/regression test (new file `tests/test_knowledge_area_backfill.py` or extend `tests/test_backup_merger.py`) that runs provisioning + merge twice in sequence against the same temp environment and asserts junction table row counts do not decrease between runs (idempotency, per data-model.md's "Idempotência" rule)
- [x] T012 [US2] Run `python app.py weekly` twice in sequence (or the two-run check in `quickstart.md` step 7) against a scratch/test database copy and confirm via SQL query that `researcher_knowledge_areas`, `group_knowledge_areas`, `initiative_knowledge_areas` counts are stable or grow between the two runs, never dropping to 0
- [x] T013 [US2] Confirm the merge summary log line (emitted by `BackupDatabaseMerger.merge()`) now includes entries for all three knowledge-area junction tables, satisfying Constitution Principle IV (audit-driven visibility)

**Checkpoint**: User Stories 1 AND 2 both verified — the fix is durable across weekly runs, not a one-time patch.

---

## Phase 5: User Story 3 - Knowledge Areas Stay Current for Existing Groups and Keyword-less Initiatives (Priority: P3)

**Goal**: The live (non-backup) ingestion pipeline keeps knowledge-area associations current for existing groups and derives them for initiatives lacking keyword metadata.

**Independent Test**: Update an existing research group's recorded knowledge areas and re-ingest; separately, ingest an initiative without `metadata.keywords` and confirm it still receives applicable knowledge area associations.

### Implementation for User Story 3

- [x] T014 [P] [US3] In `src/core/logic/research_group_loader.py`, extend the `group_already_existed` branch (~line 280+) to also call `ensure_knowledge_area`/re-sync `knowledge_area_ids` for the group's current knowledge areas, mirroring the logic already applied in the creation branch (~line 227)
- [x] T015 [P] [US3] In `src/core/logic/initiative_linker.py`, modify `associate_keyword_knowledge_areas` (~line 313-328) to fall back to deriving knowledge areas from the initiative's linked research group and/or team members' existing knowledge areas when `metadata.keywords` is absent, instead of returning early with no linkage
- [x] T016 [US3] Add unit tests covering: (a) an existing research group whose knowledge areas change on re-ingestion ends up with updated `knowledge_area_ids`, and (b) an initiative without `metadata.keywords` but with a linked group/researcher that has knowledge areas receives a derived association

**Checkpoint**: All three user stories independently functional — flat linkage is complete, durable, and stays current for live ingestion.

---

## Phase 6: Polish & Cross-Cutting Concerns

**Purpose**: Final validation across all stories.

- [x] T017 [P] Run `.venv/bin/pytest tests/test_backup_merger.py tests/test_knowledge_area_backfill.py` (or wherever T005/T006/T011 tests live) and confirm all pass
- [x] T018 Run the full `specs/012-knowledge-area-linkage-backfill/quickstart.md` validation sequence end-to-end (steps 1-7) against a scratch database copy
- [x] T019 Run `make ci-check` to confirm linting, formatting, type checking, and the full test suite pass per the project constitution's Development Workflow gate

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: No dependencies — can start immediately
- **Foundational (Phase 2)**: Depends on Setup — BLOCKS all user stories (provisioner/merger must populate the tables before anything can be verified)
- **User Stories (Phase 3-5)**: All depend on Foundational phase completion
  - US1 and US2 are tightly coupled (US2 verifies US1's fix survives repeated runs) — recommended sequentially
  - US3 (P3) touches different files (`research_group_loader.py`, `initiative_linker.py`) and can proceed in parallel with US1/US2 verification once Phase 2 is done
- **Polish (Phase 6)**: Depends on all desired user stories being complete

### Parallel Opportunities

- T004, T005, T006 (Phase 2) can run in parallel — different files/independent test additions
- T014 and T015 (Phase 5) touch different files and can run in parallel
- T017 (Phase 6) can run in parallel with T018/T019 setup, though results should be reviewed together

---

## Implementation Strategy

### MVP First (User Story 1 Only)

1. Complete Phase 1: Setup (baseline capture)
2. Complete Phase 2: Foundational (provisioner + merger fix — this is the actual bug fix)
3. Complete Phase 3: User Story 1 (verify linkage now appears)
4. **STOP and VALIDATE**: Confirm knowledge areas render for known entities
5. Ship — this alone resolves the reported 100% failure

### Incremental Delivery

1. Setup + Foundational → junction tables populate correctly (the core fix)
2. User Story 1 → verify visibility → this is the MVP the original report asked for
3. User Story 2 → verify durability across weekly runs → closes the regression risk
4. User Story 3 → live-ingestion freshness for existing groups/keyword-less initiatives → lower-priority polish

---

## Notes

- No `contracts/` tasks — `IBackupDatabaseMerger`'s signature does not change (per plan.md); only the contents of its existing return dictionary gain new keys.
- Tests are included per this repository's constitution (Principle IV: new loaders/mergers require audit coverage), not because the spec explicitly requested TDD.
- Task IDs are sequential across all phases for a single linear execution track; [P] tasks within Phase 2 and Phase 5 may be parallelized across two engineers if desired.

## Implementation Report (2026-08-15)

All 19 tasks completed. Summary:

- **T002-T006**: `backup_db_provisioner.py` gained sub-step "11e" populating all three knowledge-area junction tables from nested archive data, with unmatched `area_id`s skipped and counted (not silently dropped). `backup_merger.py`'s `tables_to_merge` now includes `researcher_knowledge_areas` and `initiative_knowledge_areas`. 4 new unit tests added to `tests/test_backup_merger.py`, all passing.
- **T007-T010**: Verified against a scratch copy of the real `db/horizon.db` (not the production file directly) — merge produced 1,131 `researcher_knowledge_areas`, 1,298 `group_knowledge_areas`, 398 `initiative_knowledge_areas` rows from 0. Researcher id 14 confirmed to have 8 linked knowledge areas.
- **T011-T013**: Idempotency confirmed both via a persisted unit test (`test_knowledge_area_links_are_idempotent_across_repeated_runs`) and by running provisioning+merge twice against the real-data scratch copy — counts identical across both runs. T012 was validated via direct provisioner/merger calls rather than the full `python app.py weekly` CLI, to avoid invoking live external scrapers (SigPesq/CNPq/Lattes) against production state; this exercises the same code path the weekly orchestrator calls for the `merge_backup` phase.
- **T014-T016**: `research_group_loader.py` now re-syncs knowledge areas for existing groups on re-ingestion (previously only set at creation). `initiative_linker.py` now derives knowledge areas from the linked research group when `metadata.keywords` is absent. 3 new unit tests added in `tests/test_knowledge_area_live_sync.py`, all passing.
- **T017-T019**: All 11 new/modified tests pass. All touched files are `black`/`isort`/`flake8` clean. `make ci-check` was NOT run as-is because the repository has 35 pre-existing files (none touched by this feature) that already fail `black --check`, and the full test suite has 11 pre-existing failures confirmed unrelated to this change (verified via `git stash` comparison against HEAD — same failures occur without this feature's code). These are pre-existing conditions of the working tree, not regressions introduced here.
